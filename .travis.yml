sudo: false
language: python
dist: "xenial"
python:
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install tox codecov
# command to run tests
script:
  - tox -v
after_success:
  - codecov
